{
    "name":  "my-jamine-project",
    "private":  true,
    "workspaces":  [
                       "packages/*"
                   ],
    "scripts": {
                    "postinstall": "npm run build -w @shared/dto",
                    "dev:frontend":  "npm run dev -w frontend",
                    "dev:backend":  "npm run start:dev -w backend",
                    "install:all":  "npm install",
                     "build:backend":  "cd packages/backend && npx prisma generate && cd ../../ && npm run build -w backend", "build:backend:ci":  "cd packages/backend && npx prisma generate && cd ../../ && npm run build:ci -w backend",
                    "start:backend":  "npm run start:prod -w backend"
                }
}
